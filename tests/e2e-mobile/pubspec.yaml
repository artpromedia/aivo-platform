name: e2e_mobile_tests
description: E2E tests for AIVO mobile apps using Patrol

publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: '>=3.19.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  patrol: ^3.13.0
  integration_test:
    sdk: flutter
  http: ^1.2.0
  shared_preferences: ^2.2.2
  mocktail: ^1.0.3
