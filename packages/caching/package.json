{
  "name": "@aivo/caching",
  "version": "1.0.0",
  "description": "Multi-layer caching service for AIVO platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "lru-cache": "^10.1.0",
    "@aivo/ts-observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  },
  "peerDependencies": {
    "@aivo/ts-observability": "workspace:*"
  }
}
