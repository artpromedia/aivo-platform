{
  "name": "@aivo/database",
  "version": "1.0.0",
  "description": "Database utilities and query optimization for AIVO platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "@aivo/ts-observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prisma": "^5.7.0",
    "typescript": "^5.3.3",
    "vitest": "^1.1.0"
  }
}
