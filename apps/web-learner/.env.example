# =============================================================================
# AIVO Web Learner — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in values for your environment.

# ── App ──────────────────────────────────────────────────────────────────────
NODE_ENV=development

# ── API (optional — learner-app uses minimal server-side env vars) ───────────
# Performance measurement endpoint (optional)
# NEXT_PUBLIC_PERFORMANCE_API_URL=http://localhost:4000
