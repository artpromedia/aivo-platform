apiVersion: apps/v1
kind: Deployment
metadata:
  name: cognitive-load-svc
  namespace: aivo-prod
  labels:
    app: cognitive-load-svc
    tier: python-light
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cognitive-load-svc
  template:
    metadata:
      labels:
        app: cognitive-load-svc
        tier: python-light
    spec:
      containers:
        - name: cognitive-load-svc
          image: aivo/cognitive-load-svc
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: python-ai-secrets
          resources:
            requests:
              cpu: 100m
              memory: 512Mi
            limits:
              cpu: 500m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: cognitive-load-svc
  namespace: aivo-prod
spec:
  selector:
    app: cognitive-load-svc
  ports:
    - port: 8000
      targetPort: 8000
