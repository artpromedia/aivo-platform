apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledge-graph-svc
  namespace: aivo-prod
  labels:
    app: knowledge-graph-svc
    tier: python-medium
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledge-graph-svc
  template:
    metadata:
      labels:
        app: knowledge-graph-svc
        tier: python-medium
    spec:
      containers:
        - name: knowledge-graph-svc
          image: aivo/knowledge-graph-svc
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: python-ai-secrets
          resources:
            requests:
              cpu: 250m
              memory: 1Gi
            limits:
              cpu: "1000m"
              memory: 2Gi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: knowledge-graph-svc
  namespace: aivo-prod
spec:
  selector:
    app: knowledge-graph-svc
  ports:
    - port: 8000
      targetPort: 8000
