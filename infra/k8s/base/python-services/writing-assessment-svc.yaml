apiVersion: apps/v1
kind: Deployment
metadata:
  name: writing-assessment-svc
  namespace: aivo-prod
  labels:
    app: writing-assessment-svc
    tier: python-medium
spec:
  replicas: 1
  selector:
    matchLabels:
      app: writing-assessment-svc
  template:
    metadata:
      labels:
        app: writing-assessment-svc
        tier: python-medium
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: writing-assessment-svc
          image: aivo/writing-assessment-svc:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: python-ai-secrets
          resources:
            requests:
              cpu: 250m
              memory: 1Gi
              ephemeral-storage: 128Mi
            limits:
              cpu: "1000m"
              memory: 2Gi
              ephemeral-storage: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 40
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: writing-assessment-svc
  namespace: aivo-prod
spec:
  selector:
    app: writing-assessment-svc
  ports:
    - port: 8000
      targetPort: 8000
