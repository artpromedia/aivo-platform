apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-inference-svc
  namespace: aivo-prod
  labels:
    app: ai-inference-svc
    tier: python-light
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-inference-svc
  template:
    metadata:
      labels:
        app: ai-inference-svc
        tier: python-light
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
      containers:
        - name: ai-inference-svc
          image: aivo/ai-inference-svc:latest
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: python-ai-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: python-ai-secrets
                  key: ai-inference-database-url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
              ephemeral-storage: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
              ephemeral-storage: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ai-inference-svc
  namespace: aivo-prod
spec:
  selector:
    app: ai-inference-svc
  ports:
    - port: 8000
      targetPort: 8000
