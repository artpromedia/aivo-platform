apiVersion: apps/v1
kind: Deployment
metadata:
  name: python-api-gateway
  namespace: aivo-prod
  labels:
    app: python-api-gateway
    tier: python-light
spec:
  replicas: 1
  selector:
    matchLabels:
      app: python-api-gateway
  template:
    metadata:
      labels:
        app: python-api-gateway
        tier: python-light
    spec:
      containers:
        - name: python-api-gateway
          image: aivo/python-api-gateway
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: python-ai-secrets
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: python-ai-secrets
                  key: gateway-database-url
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: python-api-gateway
  namespace: aivo-prod
spec:
  selector:
    app: python-api-gateway
  ports:
    - port: 8000
      targetPort: 8000
