apiVersion: apps/v1
kind: Deployment
metadata:
  name: rl-tutoring-svc
  namespace: aivo-prod
  labels:
    app: rl-tutoring-svc
    tier: python-light
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rl-tutoring-svc
  template:
    metadata:
      labels:
        app: rl-tutoring-svc
        tier: python-light
    spec:
      containers:
        - name: rl-tutoring-svc
          image: aivo/rl-tutoring-svc
          ports:
            - containerPort: 8000
          envFrom:
            - secretRef:
                name: python-ai-secrets
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: rl-tutoring-svc
  namespace: aivo-prod
spec:
  selector:
    app: rl-tutoring-svc
  ports:
    - port: 8000
      targetPort: 8000
