apiVersion: v1
kind: Service
metadata:
  name: {{ include "aivo-service.fullname" . }}
  labels:
    {{- include "aivo-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aivo-service.selectorLabels" . | nindent 4 }}
