apiVersion: apps/v1
kind: Deployment
metadata:
  name: residency-svc
  labels:
    app: residency-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: residency-svc
  template:
    metadata:
      labels:
        app: residency-svc
    spec:
      containers:
        - name: residency-svc
          image: aivo/residency-svc:latest
          ports:
            - containerPort: 4072
          envFrom:
            - configMapRef:
                name: residency-svc-config
            - secretRef:
                name: residency-svc-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4072
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4072
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: residency-svc
spec:
  selector:
    app: residency-svc
  ports:
    - port: 80
      targetPort: 4072
