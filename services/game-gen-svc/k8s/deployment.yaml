apiVersion: apps/v1
kind: Deployment
metadata:
  name: game-gen-svc
  labels:
    app: game-gen-svc
spec:
  replicas: 2
  selector:
    matchLabels:
      app: game-gen-svc
  template:
    metadata:
      labels:
        app: game-gen-svc
    spec:
      containers:
        - name: game-gen-svc
          image: aivo/game-gen-svc:latest
          ports:
            - containerPort: 4077
          envFrom:
            - configMapRef:
                name: game-gen-svc-config
            - secretRef:
                name: game-gen-svc-secrets
          resources:
            requests:
              memory: "512Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 4077
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 4077
            initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: game-gen-svc
spec:
  selector:
    app: game-gen-svc
  ports:
    - port: 80
      targetPort: 4077
