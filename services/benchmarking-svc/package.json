{
  "name": "@aivo/benchmarking-svc",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-district benchmarking and performance comparison service",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "(prisma generate || true) && tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "db:migrate": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "fastify": "^4.28.1",
    "fastify-plugin": "^4.5.1",
    "@fastify/cors": "^9.0.1",
    "@fastify/helmet": "^11.1.1",
    "@fastify/jwt": "^8.0.1",
    "@fastify/rate-limit": "^9.1.0",
    "jose": "^5.2.4",
    "zod": "^3.23.8",
    "pino": "^9.5.0",
    "date-fns": "^3.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "prisma": "^5.22.0",
    "tsx": "^4.19.2",
    "typescript": "^5.5.2",
    "vitest": "^2.1.8"
  },
  "overrides": {
    "fastify": "^4.28.1"
  }
}
